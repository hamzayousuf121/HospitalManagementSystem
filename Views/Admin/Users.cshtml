@using WebApplication2.Models;
@model List<User>

<div class="container">
    <div class="row">
        <h1 class="text-danger mt-3 text-center">User</h1>

        <div class="col-lg-12">
            <a asp-controller="Admin" asp-action="AddOrUpdateUser" class="text-white btn btn-danger border btn-block">
                Add User
            </a>

            <table class="table table-hover mt-3 table-striped">
                <tbody>
                    <tr>
                        <th>Name</th>
                        <th>Email</th>
                        <th>Phone Number</th>
                        <th>Joined On</th>
                        <th>Role</th>
                        <th>Action</th>
                    </tr>
                    @if (Model != null && Model.Count > 0)
                    {
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    @item.Name

                                </td>
                                <td>
                                    @item.Email
                                </td>
                                <td>
                                    @item.PhoneNumber
                                </td>
                                <td>
                                    @item.JoinOn
                                </td>
                                  <td>
                                    @item.Role.Name
                                </td>
                                <td>
                                    <button class="btn btn-warning">
                                        <a asp-controller="Admin" asp-action="AddOrUpdateUser" class="text-white">
                                            Edit
                                        </a>
                                    </button>
                                    <button class="btn btn-danger" asp-controller="Admin" asp-action="DeleteUser"
                                    asp-route-id="@item.Id">
                                        Delete
                                    </button>
                                </td>
                            </tr>

                        }
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
